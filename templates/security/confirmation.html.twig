{% extends 'base.html.twig' %}

{% block body %}

    {% if user %}
        <h5>Congratulations!</h5>
        <p>Your account is confirmed!</p>
        <p>You can sign in <a href="{{ path('security_login') }}">here</a>.</p>
    {% else %}
        <p>Something went wrong. Please check your e-mail.</p>
    {% endif %}

{% endblock %}
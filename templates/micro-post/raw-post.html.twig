        <div class="media text-muted pt-3">
        {# splits the name into array atributes first initial checks last initial to atribute #}
        {% set initials = post.user.fullName|split(' ') %}
        {% set first_name_letter = attribute(initials, 0)|first %}
        {% if initials[1] is defined %}
            {% set last_name_letter = attribute(initials, 1)|first %}
        {% else %}
            {% set last_name_letter = "" %}
        {% endif %}

            <img data-src="holder.js/32x32?text={{ first_name_letter }}{{ last_name_letter }}&bg=e83e8c&fg=fff&size=8" alt="" class="mr-2 rounded">
            <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
                <span class="d-block">
                <a href="{{ path('micro_post_user', {'username': post.user.username}) }}">
                    <strong class="text-gray-dark">@{{ post.user.username }}</strong> 
                </a>
                    <small>· {{ post.time|date("d/m/Y") }}</small></span>
                <a href="{{ path('micro_post_post', {'id': post.id}) }}">
                    {{ post.text }}
                </a>
            </p>
        </div>